layer: 'application'
tags:
  - 'aws'
  - 'tofu'

project:
  title: 'AWS Keycloak'
  description: 'OpenTofu stack for the AWS EC2 Keycloak instance.'
  owner: 'GilmanLab'
  maintainers:
    - 'josh'

tasks:
  check:
    command: './scripts/check.sh'
    toolchains: 'system'
    inputs:
      - '*.tf'
      - '*.tfvars'
      - '.terraform.lock.hcl'
      - 'scripts/check.sh'
      - 'templates/**/*.tftpl'
      - 'tests/**/*.tftest.hcl'
    options:
      cache: false
      runInCI: true
